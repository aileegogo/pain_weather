<style>
  /* 1. 안티그래비티 테마 배경 및 폰트 */
  :root {
    --primary-green: #2D5A27;
    --soft-green: #A8D5BA;
    --bg-gradient: linear-gradient(135deg, #f0f7f2 0%, #ffffff 100%);
    --card-shadow: 0 10px 30px rgba(45, 90, 39, 0.08);
  }

  body {
    background: var(--bg-gradient);
    font-family: 'Pretendard', -apple-system, sans-serif;
    margin: 0;
    padding: 0;
    color: #333;
  }

  .main-container {
    max-width: 900px;
    margin: 0 auto;
    padding: 40px 20px;
    box-sizing: border-box;
    min-height: 100vh;
    display: flex;
    flex-direction: column;
  }

  /* 2. 멋진 상단 헤더 디자인 복원 */
  .main-header {
    text-align: center;
    margin-bottom: 50px;
  }

  .main-header h1 {
    font-size: 36px;
    font-weight: 800;
    color: var(--primary-green);
    letter-spacing: -1px;
    margin-bottom: 10px;
    text-shadow: 2px 2px 4px rgba(0,0,0,0.05);
  }

  /* 3. 입체적인 대시보드 카드 디자인 */
  .dashboard-content {
    flex: 1;
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 25px;
    width: 100%;
  }

  .info-card {
    background: rgba(255, 255, 255, 0.9);
    backdrop-filter: blur(10px);
    padding: 35px;
    border-radius: 25px;
    border: 1px solid rgba(168, 213, 186, 0.3);
    box-shadow: var(--card-shadow);
    transition: transform 0.3s ease;
  }

  .info-card:hover {
    transform: translateY(-5px);
  }

  .info-card h3 {
    color: var(--primary-green);
    font-size: 20px;
    margin-top: 0;
    display: flex;
    align-items: center;
    gap: 10px;
  }

  /* 4. 절대 우측으로 붙지 않는 하단 고정 푸터 */
  footer {
    clear: both !important;
    width: 100% !important;
    margin-top: 80px !important;
    padding: 40px 0 !important;
    border-top: 1px solid #eef2ef !important;
    text-align: center !important;
  }

  /* 5. 모바일 최적화 (세로 정렬 강제) */
  @media (max-width: 600px) {
    .main-header h1 { font-size: 28px; }
    .dashboard-content { grid-template-columns: 1fr; }
    .info-card { padding: 25px; }
  }

  /* 모달 디자인 복원 */
  #infoModal {
    display: none;
    position: fixed;
    z-index: 10000;
    left: 0; top: 0; width: 100%; height: 100%;
    background: rgba(0,0,0,0.5);
    backdrop-filter: blur(8px);
  }

  .modal-content {
    background: white;
    margin: 15% auto;
    padding: 35px;
    border-radius: 30px;
    width: 85%;
    max-width: 500px;
    box-shadow: 0 20px 50px rgba(0,0,0,0.2);
  }
</style>

<div class="main-container">
  <header class="main-header">
    <h1>oneclipai.info</h1>
    <div style="width: 50px; height: 4px; background: var(--soft-green); margin: 0 auto 15px; border-radius: 2px;"></div>
    <p style="color: #666; font-size: 16px; font-weight: 500;">AI 날씨 & 건강 리포트</p>
  </header>

  <div class="dashboard-content">
    <div class="info-card">
      <h3>☀️ 실시간 기상</h3>
      <div id="weather-data" style="font-size: 22px; font-weight: 700; color: #444; margin: 20px 0;">
        춘천시 기상 분석 중...
      </div>
      <p style="font-size: 14px; color: #888;">업데이트: <%= Time.now.strftime("%H:%M") %></p>
    </div>

    <div class="info-card" style="background: linear-gradient(145deg, #ffffff, #f9fffb);">
      <h3>🏥 건강 가이드</h3>
      <div id="health-advice" style="line-height: 1.8; color: #555; font-size: 15px;">
        현재 날씨를 바탕으로 사용자님을 위한 맞춤 건강 조언을 생성하고 있습니다. 잠시만 기다려주세요.
      </div>
    </div>
  </div>

  <footer>
    <div style="margin-bottom: 20px;">
      <a href="javascript:void(0);" onclick="showModal('service')" style="text-decoration: none; color: var(--primary-green); font-size: 14px; margin: 0 15px; font-weight: 600;">서비스 소개</a>
      <span style="color: #ddd;">|</span>
      <a href="javascript:void(0);" onclick="showModal('privacy')" style="text-decoration: none; color: var(--primary-green); font-size: 14px; margin: 0 15px; font-weight: 600;">개인정보처리방침</a>
    </div>
    <p style="color: #aaa; font-size: 12px; margin: 0;">
      © 2026 <strong style="color: var(--soft-green);">oneclipai.info</strong>. All rights reserved.
    </p>
  </footer>
</div>

<%# 모달 및 스크립트는 이전과 동일하여 생략 (전체 코드에는 포함됨) %>
<div id="infoModal">
  <div class="modal-content">
    <span onclick="closeModal()" style="float: right; font-size: 28px; font-weight: bold; cursor: pointer; color: #999;">&times;</span>
    <h2 id="modalTitle" style="color: var(--primary-green); margin-top: 0; border-bottom: 2px solid var(--soft-green); padding-bottom: 15px;"></h2>
    <div id="modalContent" style="margin-top: 20px; line-height: 1.8; color: #444;"></div>
  </div>
</div>

<script>
  const contentData = {
    service: { title: "서비스 소개", body: "oneclipai.info는 AI 기반의 실시간 기상 정보와 건강 지수를 결합하여 최적화된 생활 가이드를 제공합니다." },
    privacy: { title: "개인정보처리방침", body: "본 서비스는 사용자의 지역 정보 외에 어떠한 개인정보도 저장하지 않습니다." }
  };
  function showModal(type) {
    document.getElementById('modalTitle').innerText = contentData[type].title;
    document.getElementById('modalContent').innerText = contentData[type].body;
    document.getElementById('infoModal').style.display = "block";
  }
  function closeModal() { document.getElementById('infoModal').style.display = "none"; }
</script>