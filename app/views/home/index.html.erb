<%# 1. 전체 스타일 및 모바일 최적화 %>
<style>
  /* 폰트 및 배경 설정 */
  body {
    background-color: #f8faf9;
    font-family: 'Pretendard', -apple-system, sans-serif;
    margin: 0;
    padding: 0;
  }

  .main-container {
    max-width: 800px; /* 대시보드 폭 최적화 */
    margin: 0 auto;
    padding: 20px;
    box-sizing: border-box;
    min-height: 100vh; /* 화면 전체 높이 확보 */
    display: flex;
    flex-direction: column;
  }

  /* 대시보드 카드 영역 */
  .dashboard-content {
    flex: 1; /* 푸터를 아래로 밀어내는 역할 */
    display: flex;
    flex-direction: column;
    gap: 20px;
  }

  .info-card {
    background: white;
    padding: 25px;
    border-radius: 20px;
    box-shadow: 0 4px 20px rgba(0,0,0,0.05);
    border: 1px solid #eef2ef;
    text-align: center;
  }

  /* 푸터 레이아웃 (우측 쏠림 방지 핵심) */
  .footer-area {
    clear: both !important;
    width: 100% !important;
    margin-top: 50px !important;
    padding: 40px 0 !important;
    border-top: 1px solid #eef2ef !important;
    text-align: center !important;
  }

  /* 팝업 모달 스타일 */
  #infoModal {
    display: none;
    position: fixed;
    z-index: 9999;
    left: 0;
    top: 0;
    width: 100%;
    height: 100%;
    background-color: rgba(0,0,0,0.6);
    backdrop-filter: blur(5px);
  }

  .modal-content {
    background-color: #fff;
    margin: 15% auto;
    padding: 30px;
    border-radius: 20px;
    width: 90%;
    max-width: 500px;
    position: relative;
    box-shadow: 0 10px 30px rgba(0,0,0,0.2);
    text-align: left;
  }

  @media (max-width: 600px) {
    .main-container { padding: 15px; }
    .modal-content { margin: 35% auto; }
  }
</style>

<div class="main-container">
  <%# 2. 상단 타이틀 %>
  <header style="text-align: center; margin-bottom: 40px; padding-top: 20px;">
    <h1 style="color: #2D5A27; font-size: 26px; font-weight: 800; margin-bottom: 10px;">oneclipai.info</h1>
    <p style="color: #666; font-size: 14px;">실시간 기상 및 건강 지수 통합 대시보드</p>
  </header>

  <%# 3. 대시보드 본문 (폰에서 세로로 쭉 정렬됨) %>
  <div class="dashboard-content">
    <div class="info-card">
      <h3 style="color: #4A7C44; margin-top: 0;">📍 현재 기상 정보</h3>
      <%# 기존 기상 데이터 출력 코드가 있다면 여기에 넣으세요 %>
      <p id="weather-display" style="font-size: 18px; font-weight: 600; color: #333;">데이터 로딩 중...</p>
    </div>

    <div class="info-card" style="background: linear-gradient(145deg, #ffffff, #f0f7f2);">
      <h3 style="color: #4A7C44; margin-top: 0;">🏥 AI 건강 가이드</h3>
      <p id="health-display" style="color: #555; line-height: 1.6;">현재 기상 상태에 맞춰 분석 중입니다.</p>
    </div>
  </div>

  <%# 4. 푸터 (절대 옆으로 붙지 않음) %>
  <footer class="footer-area">
    <div style="margin-bottom: 20px;">
      <a href="javascript:void(0);" onclick="showModal('service')" style="text-decoration: none; color: #4A7C44; font-size: 14px; margin: 0 12px; font-weight: 600;">서비스 소개</a>
      <span style="color: #ddd;">|</span>
      <a href="javascript:void(0);" onclick="showModal('privacy')" style="text-decoration: none; color: #4A7C44; font-size: 14px; margin: 0 12px; font-weight: 600;">개인정보처리방침</a>
    </div>
    <p style="color: #aaa; font-size: 12px; margin: 0;">
      © 2026 <strong>oneclipai.info</strong>. All rights reserved.
    </p>
  </footer>
</div>

<%# 5. 모달 창 %>
<div id="infoModal">
  <div class="modal-content">
    <span onclick="closeModal()" style="position: absolute; right: 20px; top: 15px; font-size: 24px; font-weight: bold; cursor: pointer; color: #999;">&times;</span>
    <h2 id="modalTitle" style="color: #2D5A27; margin-top: 0; border-bottom: 2px solid #A8D5BA; padding-bottom: 10px; font-size: 20px;"></h2>
    <div id="modalContent" style="margin-top: 20px; line-height: 1.8; color: #444; font-size: 14px;"></div>
  </div>
</div>

<%# 6. 제어 스크립트 %>
<script>
  const contentData = {
    service: {
      title: "서비스 소개",
      body: "oneclipai.info는 AI 기반의 실시간 기상 데이터와 개인 건강 지수를 통합 분석하여, 사용자에게 최적화된 생활 가이드를 제공하는 혁신적인 대시보드 서비스입니다."
    },
    privacy: {
      title: "개인정보처리방침",
      body: "본 서비스는 사용자의 검색 편의를 위해 쿠키를 활용할 수 있으며, 입력하신 지역 정보 외에 어떠한 개인정보도 수집하거나 저장하지 않습니다. 애드센스 광고 최적화를 위해 비식별 데이터가 활용될 수 있습니다."
    }
  };

  function showModal(type) {
    document.getElementById('modalTitle').innerText = contentData[type].title;
    document.getElementById('modalContent').innerText = contentData[type].body;
    document.getElementById('infoModal').style.display = "block";
  }

  function closeModal() {
    document.getElementById('infoModal').style.display = "none";
  }

  window.onclick = function(event) {
    if (event.target == document.getElementById('infoModal')) {
      closeModal();
    }
  }
</script>