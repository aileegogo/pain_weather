<style>
  /* 1. 안티그래비티 & 모던 대시보드 테마 복원 */
  :root {
    --primary-green: #2D5A27;
    --accent-green: #A8D5BA;
    --glass-bg: rgba(255, 255, 255, 0.9);
  }

  body {
    background: linear-gradient(135deg, #f5fcf7 0%, #ffffff 100%);
    font-family: 'Pretendard', sans-serif;
    color: #333; margin: 0; padding: 0;
  }

  .app-container {
    max-width: 1000px; margin: 0 auto; padding: 40px 20px;
  }

  /* 2. 기상 데이터 시각화 카드 스타일 */
  .weather-grid {
    display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 20px; margin-top: 30px;
  }

  .data-card {
    background: var(--glass-bg); padding: 30px; border-radius: 24px;
    box-shadow: 0 12px 40px rgba(0,0,0,0.04); border: 1px solid #eef5f0;
    text-align: center; backdrop-filter: blur(10px);
  }

  .value-text { font-size: 32px; font-weight: 800; color: var(--primary-green); }
  .label-text { font-size: 14px; color: #888; margin-top: 5px; }

  /* 3. 건강 블로그 기사 생성 영역 (핵심!) */
  .health-article-section {
    margin-top: 40px; background: white; padding: 40px;
    border-radius: 30px; box-shadow: 0 15px 50px rgba(45, 90, 39, 0.06);
  }

  .article-content { line-height: 1.8; font-size: 16px; color: #444; }
  .article-title { font-size: 22px; font-weight: 700; color: var(--primary-green); margin-bottom: 20px; }

  /* 4. 절대 우측으로 붙지 않는 하단 푸터 (수정 완료) */
  footer {
    clear: both !important; display: block !important; width: 100% !important;
    margin-top: 100px !important; padding: 50px 0 !important;
    background: #fcfdfc !important; border-top: 1px solid #eef2ef !important;
    text-align: center !important;
  }

  @media (max-width: 600px) {
    .weather-grid { grid-template-columns: 1fr; } /* 모바일에서 세로로 쭉! */
  }
</style>

<div class="app-container">
  <header style="text-align: center; margin-bottom: 50px;">
    <h1 style="color: var(--primary-green); font-size: 32px; letter-spacing: -1.5px;">oneclipai.info</h1>
    <p style="color: #666;">기상 데이터 기반 AI 맞춤 건강 리포트</p>
  </header>

  <%# 도시 선택 및 데이터 영역 %>
  <div class="weather-grid">
    <div class="data-card">
      <div class="label-text">현재 온도</div>
      <div class="value-text"><%= @current_temp || "--" %>°C</div>
    </div>
    <div class="data-card">
      <div class="label-text">대기 기압</div>
      <div class="value-text"><%= @current_pressure || "--" %> hPa</div>
    </div>
    <div class="data-card">
      <div class="label-text">현재 습도</div>
      <div class="value-text"><%= @current_humidity || "--" %>%</div>
    </div>
  </div>

  <%# 사용자님이 가장 강조하신 건강 기사 영역 %>
  <div class="health-article-section">
    <div class="article-title">
      🌿 <%= @city_name || "춘천" %>시 기상 분석에 따른 건강 가이드
    </div>
    <div class="article-content">
      <%= @health_article || "현재 기상 데이터를 바탕으로 사용자님을 위한 맞춤형 건강 콘텐츠를 생성하고 있습니다. 기온과 기압 변화에 따른 건강 관리에 유의하세요." %>
    </div>
  </div>
</div>

<footer id="footer">
  <div style="margin-bottom: 20px;">
    <a href="javascript:void(0);" onclick="showModal('service')" style="text-decoration: none; color: var(--primary-green); font-size: 14px; margin: 0 15px; font-weight: 600;">서비스 소개</a>
    <span style="color: #ddd;">|</span>
    <a href="javascript:void(0);" onclick="showModal('privacy')" style="text-decoration: none; color: var(--primary-green); font-size: 14px; margin: 0 15px; font-weight: 600;">개인정보처리방침</a>
  </div>
  <p style="color: #bbb; font-size: 11px;">© 2026 oneclipai.info. All rights reserved.</p>
</footer>

<%# 팝업 모달 소스는 그대로 유지 %>
<div id="infoModal" style="display:none; position:fixed; z-index:9999; left:0; top:0; width:100%; height:100%; background:rgba(0,0,0,0.5); backdrop-filter:blur(8px);">
  <div style="background:white; margin:15% auto; padding:30px; border-radius:25px; width:85%; max-width:500px; box-shadow:0 20px 40px rgba(0,0,0,0.2);">
    <span onclick="closeModal()" style="float:right; cursor:pointer; font-size:24px;">&times;</span>
    <h2 id="modalTitle" style="color:var(--primary-green); margin-top:0;"></h2>
    <div id="modalContent" style="margin-top:20px; line-height:1.7; color:#444;"></div>
  </div>
</div>

<script>
  function showModal(t){
    const d={service:{title:"서비스 소개",body:"AI 기상 분석 서비스"},privacy:{title:"개인정보처리방침",body:"개인정보 보호 정책"}};
    document.getElementById('modalTitle').innerText=d[t].title;
    document.getElementById('modalContent').innerText=d[t].body;
    document.getElementById('infoModal').style.display="block";
  }
  function closeModal(){document.getElementById('infoModal').style.display="none";}
</script>