<style>
  /* 1. 안티그래비티 디자인 시스템: 그림 같은 배경과 색감 */
  :root {
    --primary: #2D5A27;
    --accent: #A8D5BA;
    --glass: rgba(255, 255, 255, 0.85);
    --shadow: 0 20px 60px rgba(45, 90, 39, 0.1);
  }

  body {
    background: radial-gradient(circle at top left, #f0fdf4, #ffffff 70%);
    font-family: 'Pretendard', sans-serif;
    color: #333; margin: 0; min-height: 100vh;
  }

  .main-wrapper {
    max-width: 1100px; margin: 0 auto; padding: 60px 20px;
    display: flex; flex-direction: column;
  }

  /* 2. 공중에 떠 있는 듯한(Anti-Gravity) 카드 디자인 */
  .card-grid {
    display: grid; grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: 30px; margin-bottom: 50px;
  }

  .glass-card {
    background: var(--glass); backdrop-filter: blur(15px);
    padding: 40px; border-radius: 35px; border: 1px solid rgba(168, 213, 186, 0.3);
    box-shadow: var(--shadow); transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
    text-align: center;
  }

  .glass-card:hover { transform: translateY(-10px); box-shadow: 0 30px 80px rgba(45, 90, 39, 0.15); }

  .stat-val { font-size: 42px; font-weight: 900; color: var(--primary); letter-spacing: -1px; }
  .stat-unit { font-size: 18px; color: var(--accent); margin-left: 2px; }

  /* 3. 건강 블로그 기사 영역: 애드센스 승인의 핵심 디자인 */
  .article-container {
    background: white; padding: 50px; border-radius: 40px;
    box-shadow: 0 30px 100px rgba(0,0,0,0.03); border: 1px solid #f0f5f1;
  }

  .article-header {
    font-size: 26px; font-weight: 800; color: var(--primary);
    margin-bottom: 30px; line-height: 1.4;
  }

  /* 4. 절대 우측으로 붙지 않는 하단 푸터 (완벽 격리) */
  footer {
    clear: both !important; width: 100% !important;
    margin-top: 100px !important; padding: 60px 0 !important;
    border-top: 1px solid #eee !important; text-align: center !important;
  }

  @media (max-width: 600px) {
    .card-grid { grid-template-columns: 1fr; }
    .main-wrapper { padding: 30px 15px; }
    .article-container { padding: 30px; }
  }
</style>

<div class="main-wrapper">
  <header style="text-align: center; margin-bottom: 80px;">
    <h1 style="font-size: 42px; color: var(--primary); font-weight: 900; margin-bottom: 10px;">oneclipai.info</h1>
    <div style="width: 60px; height: 6px; background: var(--accent); margin: 0 auto 20px; border-radius: 10px;"></div>
    <p style="color: #666; font-size: 18px; font-weight: 500;">기상 데이터 기반 맞춤형 건강 큐레이션</p>
  </header>

  <div class="card-grid">
    <div class="glass-card">
      <div style="font-size: 14px; color: #888; margin-bottom: 10px; font-weight: 600;">THERMOMETER</div>
      <div class="stat-val"><%= @current_temp || "--" %><span class="stat-unit">°C</span></div>
    </div>
    <div class="glass-card">
      <div style="font-size: 14px; color: #888; margin-bottom: 10px; font-weight: 600;">BAROMETER</div>
      <div class="stat-val"><%= @current_pressure || "--" %><span class="stat-unit">hPa</span></div>
    </div>
    <div class="glass-card">
      <div style="font-size: 14px; color: #888; margin-bottom: 10px; font-weight: 600;">HUMIDITY</div>
      <div class="stat-val"><%= @current_humidity || "--" %><span class="stat-unit">%</span></div>
    </div>
  </div>

  <div class="article-container">
    <div class="article-header">
      🌿 AI 실시간 분석: <span style="color: var(--accent);"><%= @city_name || "춘천" %></span> 지역 건강 리포트
    </div>
    <div id="content-body" style="line-height: 2.0; font-size: 16px; color: #444; text-align: justify;">
      <%= raw @health_article || "현재 기상 데이터를 바탕으로 고퀄리티 건강 기사를 생성하고 있습니다..." %>
    </div>
  </div>

  <footer>
    <div style="margin-bottom: 25px;">
      <a href="javascript:void(0);" onclick="showModal('service')" style="text-decoration: none; color: var(--primary); font-size: 14px; margin: 0 15px; font-weight: 600;">서비스 소개</a>
      <span style="color: #ddd;">|</span>
      <a href="javascript:void(0);" onclick="showModal('privacy')" style="text-decoration: none; color: var(--primary); font-size: 14px; margin: 0 15px; font-weight: 600;">개인정보처리방침</a>
    </div>
    <p style="color: #aaa; font-size: 11px;">© 2026 oneclipai.info. All rights reserved.</p>
  </footer>
</div>

<%# 모달 구조 완벽 복원 %>
<div id="infoModal" style="display:none; position:fixed; z-index:9999; left:0; top:0; width:100%; height:100%; background:rgba(0,0,0,0.4); backdrop-filter:blur(10px);">
  <div style="background:white; margin:10% auto; padding:40px; border-radius:35px; width:85%; max-width:500px; box-shadow:0 30px 80px rgba(0,0,0,0.3);">
    <span onclick="closeModal()" style="float:right; cursor:pointer; font-size:30px; color:#ccc;">&times;</span>
    <h2 id="modalTitle" style="color:var(--primary); margin-top:0; border-bottom:3px solid var(--accent); padding-bottom:15px; font-weight:800;"></h2>
    <div id="modalContent" style="margin-top:30px; line-height:1.9; color:#555; font-size:15px;"></div>
  </div>
</div>

<script>
  function showModal(t){
    const d={service:{title:"서비스 소개",body:"oneclipai.info는 AI 기상 분석을 통해 건강 가이드를 제공하는 프리미엄 대시보드입니다."},privacy:{title:"개인정보처리방침",body:"본 서비스는 어떠한 개인정보도 무단 수집하지 않음을 약속드립니다."}};
    document.getElementById('modalTitle').innerText=d[t].title;
    document.getElementById('modalContent').innerText=d[t].body;
    document.getElementById('infoModal').style.display="block";
  }
  function closeModal(){document.getElementById('infoModal').style.display="none";}
</script>